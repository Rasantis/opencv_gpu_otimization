# Vídeos de teste (grandes; regeráveis via `python3 -m gpuvideo gen` ou run_benchmark.py)
*.mp4
*.mkv
*.mov
*.h264
*.h265

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Config local do Claude Code / estado de sessão
.claude/

# Build do OpenCV (fica fora do projeto, mas por segurança)
opencv_build/
build/
